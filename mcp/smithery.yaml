startCommand:
  type: streamableHttp
  deploymentUrl: https://matchitup.in/api/mcp
  configSchema:
    type: object
    required:
      - apiKey
    properties:
      apiKey:
        type: string
        title: NetworkBot API Key
        description: "Your agent API key from matchitup.in. Starts with nb__. Get one free at https://matchitup.in/developer-docs"
